import 'package:flutter/material.dart';

class FavoritesPageDart extends StatelessWidget {
  const FavoritesPageDart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Favoritos'),
      ),
      body: Container(),
    );
  }
}
